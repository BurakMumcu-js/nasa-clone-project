{
  "name": "nasa-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-server": "npm install --prefix dev",
    "install-client": "npm install --prefix ui",
    "install": "npm run install-server && npm run install-client",
    "server": "npm run watch --prefix dev",
    "client": "npm start --prefix ui",
    "watch": "start npm run server & start npm run client",
    "deploy": "npm run build --prefix ui && npm start --prefix dev",
    "test": "npm run test --prefix dev && npm run test --prefix ui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "morgan": "^1.10.0"
  }
}
